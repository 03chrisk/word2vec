[project]
name = "word2vec"
version = "0.1.0"
description = "Word2Vec skip-gram with negative sampling in pure NumPy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
]
